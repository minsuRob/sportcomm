name: Keep Render Alive

on:
  schedule:
    - cron: "*/13 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render Server
        run: |
          curl -v "$RENDER_BE_URL/health"
