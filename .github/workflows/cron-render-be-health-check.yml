name: Keep Render Alive

on:
  schedule:
    - cron: "*/13 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render Server
        run: |
          curl -s -o /dev/null -w "%{http_code}" "$RENDER_BE_URL"/health
